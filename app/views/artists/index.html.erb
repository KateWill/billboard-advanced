<h1>Artists</h1>

<% @artists.each do |a| %>
<ul>
  <li><%= link_to a.name, artist_path(a) %>  |  <%= link_to 'Edit Artist Title', edit_artist_path(a) %>  |  <%= link_to "Delete Artist", a, method: :delete  %></li>
</ul>
<% end %>
